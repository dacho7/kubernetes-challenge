apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: challenge-ns
spec:
  selector:
    app: frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: challenge-ns
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
        - name: frontend
          image: gporras/chat-socket-front:latest
          env:
            - name: API_URL
              value: "http://backend:8080/chat-websocket"
          ports:
            - containerPort: 80